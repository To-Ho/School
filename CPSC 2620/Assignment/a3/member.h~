#ifndef _MEMBER_H
#define _MEMBER_H

class member{
 public:
  member();
  member(string, char, int[]);
  getName const;
  print;
  setStatus(int);
  getStatus;

 private:
  string name;
  char gender;
  int numHobbies;
  int hobby[5];
  int status;
};

#endif
