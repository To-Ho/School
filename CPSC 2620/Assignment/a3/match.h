#include "./member.h"

class match{
 public:
  match();
  ~match();
  getIndex(string, char);
  push_back;
  double computeScore(string, string);

 private:
  member* malesArray;
  member* femalesArray;
  int capF;
  int capM;
  int sizeM;
  int sizeF;
  void addCapacity(number*& array, int size, int&cap);
};

